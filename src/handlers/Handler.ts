import Type from './types/Type';
import Wrapper from './wrappers/Wrapper';

class Handler {
  public static readonly Type = Type;
  public static readonly Wrapper = Wrapper;
}

export default Handler;
